export enum CellType 
{
    Wall,
    Ground,
    Exit
}

export enum GameObjectType 
{
    Player,
    Enemy,
    Potion,
    Weapon,
    Empty
}

export enum Direction
{
    North,
    West,
    South,
    East
}